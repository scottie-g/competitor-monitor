watches:
  # Add your competitor URLs here.
  # name: display name shown in Google Sheets and Slack
  # js: true  = use a real browser (needed for React/Vue pages)
  # js: false = fast plain HTTP fetch (good for static HTML pages)

  - url: https://example.com/pricing
    name: Example Competitor
    js: false
